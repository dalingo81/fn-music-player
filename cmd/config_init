#!/bin/bash

# ══════════════════════════════════════════════════
# 配置初始化回调脚本
# ══════════════════════════════════════════════════
# 系统在「应用设置」页面打开时调用此脚本，
# 应用可以在这里输出当前配置状态信息。
# 注意：此脚本不需要保存配置，保存逻辑在 config_callback 中完成。
# ══════════════════════════════════════════════════

echo "Authorized music folders: ${TRIM_DATA_ACCESSIBLE_PATHS:-（无）}"
echo ""
echo "当您在「应用设置」中添加/删除授权文件夹后，系统会自动注入"
echo "TRIM_DATA_ACCESSIBLE_PATHS 环境变量，播放器将扫描这些路径下的音乐文件。"
exit 0
